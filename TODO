☐ Should set CWD to the directory of the tests so that a relative path
  in the .jregr can be followed wherever Jregr was run from
☐ Handle piping of output? We do input... (" < " so should we support " > "?)
☐ Remove check for executables in -bin dir(s). Maybe this should be explicitly done for each subdir?
☐ Place any error output in the .output, if already done, why does not "<bin> not found" show up there?
☐ Remove any non-XML-compliant characters when generating XML-file

Crash handling:
✔ CommandRunner: keep the value of p.waitFor(), expose it as getExitValue() @done(26-09-03 10:49)
✔ RegrCase: set fatal on signal death (exit > 128 on Unix, < 0 on Windows).
  NOT on any non-zero exit -- Alan's compiler suite is full of cases that
  legitimately exit non-zero, and that would turn 362 passing tests red @done(26-09-03 10:49)
✔ RegrCase: write a diagnostic into the .output naming the signal, so a
  fatal case says why instead of being empty @done(26-09-03 10:49)
  Reads ".jregr:1: '../crash' terminated by signal 11 (SIGSEGV)"
✔ RegrCase: break out of the command loop, so later .jregr lines do not
  run after a crash @done(26-09-03 10:49)
✔ Decide the fate of crash_should_stop_execution.expected -- deleted it.
  status() short-circuits on fatal so it could not affect the result, and
  had it ever matched the .output, clean() would have deleted the .output
  that shouldStopExecutingCommandsAfterACrash reads @done(26-09-03 10:49)
☐ Windows wording: a crash there is reported as "terminated by exception
  0xC0000005", but shouldStopExecutingCommandsAfterACrash asserts on
  "terminated by signal". Only bites when the suite is run on Windows
☐ signalName() covers the signals that Linux and macOS agree on. 7 and 10
  are left out on purpose (SIGBUS/SIGUSR1 vs SIGEMT/SIGBUS) and print as a
  bare number -- resolve per-platform if that ever matters

Test-suite defects:
✔ shouldCatchStderr asserts ": Fatal" and setUp() never compiles stderr,
  so the result depends on whether a stale gitignored binary happens to
  be on disk. Add compile("stderr") and assert ": Pass" @done(26-09-03 12:24)
  The binary on disk was from Feb 2021, and .expected holds exactly the
  stderr text, so a correct run Passes -- the test only ever went green
  when the binary was missing
✔ AcceptanceScenarios.compile() ignores cc's exit status, so a failed
  compile silently becomes a missing binary and a misleading Fatal
  @done(26-09-03 12:24) Now drains cc's diagnostics and asserts on the
  exit status, so a broken .c names itself
☐ RegrDirectoryRecursionTest mocks java.lang.Runtime, which is final as
  of ~JDK 26. Introduce a seam (RegrDirectoryDouble already takes a
  Runtime), upgrade to Mockito 5, or pin the JDK

Build:
✔ Pin JAVA_HOME. Homebrew's ant depends on openjdk and forks JDK 26,
  while java on PATH is 21 -- so 'ant test' and a hand-run java disagree
  @done(26-09-03 12:52) 'test' now forks a JDK located off
  /usr/libexec/java_home -v ${test.jdk.version}, default 21, and echoes
  which one. Override with -Dtest.jdk=<home> or -Dtest.jdk.version=N.
  Plain 'ant test' is green; -Dtest.jdk=<a 26> still gives the 4
  RegrDirectoryRecursionTest errors, so the pin is doing the work
☐ Raise -source/-target from 1.8. Deprecated on current JDKs and due to
  be removed. Nothing requires a Java 8 compatible jar -- the java8
  commits in the log are an old upgrade, not a standing constraint
✔ Narrow the 'classpath' path id from "." to "lib" -- it currently globs
  jregr.jar itself onto the compile classpath, where a stale jar can mask
  a deleted class @done(26-09-03 12:41)
  Confirmed: with src/io/File.java deleted, the old glob compiled clean
  against the copy inside jregr.jar; it now fails loudly
✔ A stale bin/ silently tests old code: javac's up-to-date check compares
  mtimes, and a fresh checkout can have sources older than old classes
  @done(26-09-03 12:41) compile and compile-tests now empty bin/ and
  test-bin/ first. Confirmed: a backdated Main.java was skipped by javac
  entirely, and bare 'ant' shipped a jar reporting the old version
✔ Release process for jregr.jar. It sat at 0.2.4 for years while the
  source said 0.2.5, and ~/bin/jregr was pointing straight at it
  @done(26-09-03 12:58) 'ant release' is now the one command: clean, then
  test, then package, then run the jar with -version so the release states
  what it actually contains. Bare 'ant' is 'build' and tests too, so no
  route to a jar skips the suite except asking for create_run_jar by name.
  Verified that a failing test produces no jar.
  Bumping JREGR_VERSION in Main.java is still a manual step beforehand,
  and ~/bin/jregr is still outside the repo's reach

☐ Publish releases on GitHub, with the jar as a release asset, instead of
  copying it by hand into whatever consumes it. The release target would
  then point at the tag rather than at a directory

Repo hygiene:
✔ Commit acceptance/crash.c; add crash and crash.exe to acceptance/.gitignore
  (theSUT and stderr are ignored, crash was missed) @done(26-09-03 10:37)
☐ .vscode/launch.json has 6 hardcoded /home/thoni paths, and
  "$(workspaceFolder)" should be "${workspaceFolder}"
☐ .settings/org.eclipse.php.core.prefs is tracked -- PHP prefs in a Java repo
☐ .DS_Store files are untracked but not ignored

✔ An empty .jregr file means the content is inherited from directory above
✔ Remove possible directory separator at end of -dir option
✔ Recurse through directories, only follow directories that have .jregr files.
✔ A top level directory should always have a .jregr file
✔ Remove source to set file encoding @done(18-12-30 12:30)
✔ Document java option -Dfile.encoding= @done(18-12-30 12:30)
✔ Fix StreamGobbler tests to compile the dummy external programs @done(18-12-30 12:52)

